#include<string.h>
//puts(character array) outputs the string
char str[]={"China\nBeijing"};
puts(str);

//gets(input string function)
gets(str);

//strcat (string concatenation function)
En =strcat(string array 1, string array 2);

Strcpy and strncpy (string copy function)
// Copy string 2 into string 1
strcpy(str1,str2);

// STRCMP (string comparison function)
STRCMP (character 1, character 2);
STRCMP (string variable 1, string variable 2);
STRCMP (character 1, string variable 2);
/* If string 1= string 2, the function value is 0
If string 1> string 2, the function value is a positive number
If string 1 is less than string 2, the function value is a negative */

//strlen(function that measures string length) (not including '\0')
Long =strlen(string array);

// STRLWR function (converted to lowercase function)
STRLWR (string)

//strupr function (converted to uppercase function)
Strupr (string)
